[package]
name = "pixel-shooter-game-core"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", default-features = false, features = ["std", "multi_threaded"] }
pixel-shooter-protocol = { path = "../../protocol" }
serde = { version = "1", features = ["derive"] }
